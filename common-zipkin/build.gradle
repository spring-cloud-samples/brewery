moduleEnabled(whatToTest('SLEUTH') || whatToTest('SLEUTH_STREAM'))

dependencies {
	compile "org.springframework.cloud:spring-cloud-starter-zipkin"
	compile "org.springframework.boot:spring-boot-starter-web"
}