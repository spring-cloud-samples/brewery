logging:
  pattern:
    level: '[trace=%X{X-Trace-Id:-},span=%X{X-Span-Id:-}] %5p'

logging.file: eureka.log

server:
  port: 8761

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
